import { motion } from "framer-motion";
import { ShoppingCart, Zap, ShieldCheck, Footprints, Sparkles } from "lucide-react";

export default function App() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 via-white to-gray-100 text-gray-900">

      {/* Navbar */}
      <header className="sticky top-0 z-50 backdrop-blur-md bg-white/70 border-b">
        <div className="max-w-6xl mx-auto flex items-center justify-between px-6 py-4">
          <h1 className="text-lg font-bold tracking-tight flex items-center gap-2">
            <Sparkles className="w-5 h-5" /> Project Condor
          </h1>
          <button className="rounded-2xl px-6 py-2 bg-black text-white">
            Pre-Order
          </button>
        </div>
      </header>

      {/* Hero */}
      <section className="px-6 py-20 max-w-6xl mx-auto">
        <motion.div
          initial={{ opacity: 0, y: 40 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.9 }}
          className="max-w-2xl"
        >
          <p className="inline-block px-4 py-2 rounded-full bg-gray-200 text-sm font-medium">
            Magnetic Golf Training Aid
          </p>

          <h2 className="text-6xl font-extrabold leading-tight mt-6">
            Keep Your Heel Grounded.
            <span className="block text-gray-600">
              Unlock Better Balance & Power.
            </span>
          </h2>

          <p className="mt-6 text-lg text-gray-700">
            Project Condor is a fully mechanical wearable training aid that gives
            instant tactile feedback if your back heel lifts too early.
            No sensors. No batteries. Just pure swing correction.
          </p>

          <div className="mt-10 flex flex-col sm:flex-row gap-4">
            <button className="rounded-2xl px-8 py-4 text-lg flex gap-2 bg-black text-white shadow-lg">
              <ShoppingCart className="w-5 h-5" /> Reserve for $55
            </button>

            <button className="rounded-2xl px-8 py-4 text-lg border">
              Watch How It Works
            </button>
          </div>

          <p className="mt-4 text-sm text-gray-500">
            Limited first production batch shipping Spring 2026.
          </p>
        </motion.div>
      </section>

      {/* Features */}
      <section className="px-6 py-20 bg-white">
        <div className="max-w-6xl mx-auto">
          <h3 className="text-4xl font-bold text-center">
            Why Golfers Love Project Condor
          </h3>

          <div className="grid md:grid-cols-3 gap-10 mt-14">

            <Feature
              icon={<Footprints className="w-7 h-7" />}
              title="Stay Grounded"
              desc="Prevent premature heel lift and maintain balance."
            />

            <Feature
              icon={<Zap className="w-7 h-7" />}
              title="Instant Feedback"
              desc="Magnets detach early if you lift too soon."
            />

            <Feature
              icon={<ShieldCheck className="w-7 h-7" />}
              title="100% Mechanical"
              desc="No electronics, charging, or apps required."
            />

          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="px-6 py-24 bg-gradient-to-r from-gray-900 to-gray-800 text-white">
        <div className="max-w-4xl mx-auto text-center">
          <h3 className="text-5xl font-extrabold">
            Ready to Improve Your Swing?
          </h3>

          <p className="mt-6 text-lg text-gray-300">
            Train proper weight transfer with instant magnetic feedback.
          </p>

          <button className="mt-10 rounded-2xl px-12 py-5 text-lg font-semibold bg-white text-black shadow-xl">
            Pre-Order for $55
          </button>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-10 text-center text-gray-500 text-sm">
        © {new Date().getFullYear()} Project Condor
      </footer>
    </div>
  );
}

function Feature({ icon, title, desc }) {
  return (
    <div className="rounded-3xl shadow-md hover:shadow-xl transition p-10">
      <div className="w-14 h-14 flex items-center justify-center rounded-2xl bg-gray-100 mb-6">
        {icon}
      </div>
      <h4 className="text-2xl font-semibold">{title}</h4>
      <p className="mt-3 text-gray-600">{desc}</p>
    </div>
  );
}
